appId: io.dynolabs.cinova
---
# Smoke gate — minimum proof the app boots, JS bundle loads, native modules
# link, and the tab navigator renders. Per-pillar walks (Reels / Discover /
# Search / Watchlist / Profile) are added as separate flows referenced here
# once each tab is stable enough for headless assertion.
- runFlow: 01-launch.yaml
